# @kelvan-shared/consts

Shared constants and feature flags for development mode behavior.

## Exports

- `./analytics` — `IGNORE_LOCAL_TRACKING` flag to bypass analytics in development
- `./emails` — `IGNORE_LOCAL_EMAILS` flag to bypass email sending in development
