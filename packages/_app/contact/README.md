# @kelvan-app/contact

Contact form React component with validation, loading states, and toast notifications.

## Exports

- `./contact-form` — `ContactForm` component (client component using atomic design pattern)

## Dependencies

- `@kelvan-api/contact` — server action and validation schema
- `@kelvan-core/next-safe-action` — action hooks
- `@kelvan-design/ui` — UI components (peer)
